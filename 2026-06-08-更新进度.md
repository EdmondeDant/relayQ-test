# 2026-06-08 更新进度记录

> 用途：本文件用于重开新会话后快速接续验证今天完成的修改。

## 1. 项目本地运行方式调整

### 背景

云电脑环境 Docker daemon 不可用，Docker Compose 启动失败。

### 处理结果

已改用 Windows 本机兼容方式运行：

- 后端：Go 本地启动
- 前端：Vite 本地启动
- 数据库：PostgreSQL 本机服务
- 缓存：Redis 本机服务

### 关键配置

- 后端目录：`c:\work\RelayQ-test\backend`
- 前端目录：`c:\work\RelayQ-test\frontend`
- 后端数据目录：`c:\work\RelayQ-test\backend\data`
- 后端配置文件：`c:\work\RelayQ-test\backend\data\config.yaml`
- 本地数据库曾改为干净库：`relayq_local_dev`

### 已处理过的问题

- Docker daemon 不可用
- migration checksum mismatch
- PostgreSQL schema 权限问题
- TOTP encryption key 非法问题
- 前端 `localhost:3000` 服务未运行导致 `ERR_CONNECTION_REFUSED`

---

## 2. 可用渠道页图片模型价格显示修复

### 用户反馈

后台设置图片价格应为 `0.04` 一张，但可用渠道页面显示为 `$0.00003 / 次`。

### 根因

前端展示的是 LiteLLM 图片 token 单价，例如：

- `image_output_price`
- `output_cost_per_image_token`

而不是后台分组配置的图片实际价格。

### 修改文件

- `backend/internal/handler/available_channel_handler.go`
- `backend/internal/service/channel_available.go`
- `frontend/src/api/channels.ts`
- `frontend/src/components/channels/SupportedModelChip.vue`

### 处理结果

- 后端用户可见模型响应中增加分组图片价格：
  - `image_price_1k`
  - `image_price_2k`
  - `image_price_4k`
- 前端图片模型优先展示分组图片价格。
- 如果没有分组图片价，回落显示 `per_request_price`，不再优先显示 token 图片单价。
- 本地 openai 分组图片价曾补为 `0.04` 方便验证。

### 验证建议

重开会话后验证：

1. 打开前端可用渠道页。
2. 找到图片模型，例如 `gpt-image-2`。
3. 确认价格不再显示 `$0.00003 / 次`。
4. 如果后台分组图片价为 `0.04`，页面应展示接近 `$0.04` 的图片价格。

---

## 3. `/home` 主页 UI 改版

### 用户需求

在不改变功能代码前提下，把 `/home` 首页改成年轻、二次元、创业风格，使用浮空彩色胶囊样式。

### 修改文件

- `frontend/src/views/HomeView.vue`

### 处理结果

- 保留原本功能逻辑：
  - 登录跳转
  - 主题切换
  - 语言切换
  - 文档入口
  - 自定义 `homeContent` 渲染逻辑
- 改造默认首页视觉：
  - 年轻化
  - 二次元科技感
  - 彩色浮空胶囊
  - Hero 区域重新排版
- 修复标题后方装饰遮挡问题。
- 页脚右侧改为：`联系 VX：Mictimeles`。

### 验证建议

1. 启动前端。
2. 访问 `http://localhost:3000/home`。
3. 检查标题没有被背景元素遮挡。
4. 检查页脚右侧显示 `联系 VX：Mictimeles`。
5. 检查登录、主题、语言等功能仍正常。

---

## 4. 注册邀请码逻辑修复

### 用户反馈

后台“邀请码注册”开关打开后，不填写有效邀请码无法注册。用户认为正常逻辑应为：

- 邀请码选填
- 有邀请码：走邀请返利/邀请码逻辑
- 没邀请码：正常注册

### 最终决策

- 适用范围：仅邮箱注册
- 返利绑定：邀请码也可作为返利码兜底

### 修改文件

- `backend/internal/service/auth_service.go`
- `frontend/src/views/auth/RegisterView.vue`
- `frontend/src/views/auth/EmailVerifyView.vue`

### 后端处理结果

在 `RegisterWithVerification` 中：

- `invitationCode` 和 `affiliateCode` 先 `TrimSpace`。
- 当邀请码注册开关开启时：
  - 只有用户填写了邀请码，才校验邀请码并消费。
  - 用户没填邀请码，不再返回 `invitation code is required`。
- 返利绑定使用处理后的 `affiliateCode`。

### 前端处理结果

注册页：

- 邀请码输入改为选填。
- 移除前端必填校验。
- 提交注册时：
  - 如果填写了邀请码，就作为 `invitation_code` 发送。
  - 如果没有推广码 `aff_code`，则把邀请码作为 `aff_code` 兜底，尝试邀请返利绑定。

验证码页：

- 修复邮箱验证码注册路径遗漏的问题。
- `EmailVerifyView.vue` 加入同样的 `aff_code` 兜底逻辑。

### 已发现的验证注意事项

如果仍出现 `invitation code is required`，优先检查：

1. 后端是不是旧进程没有重启。
2. 浏览器 `sessionStorage` 是否保存了旧的 `register_data`。

可在浏览器控制台执行：

```javascript
sessionStorage.removeItem('register_data')
```

然后 Ctrl + F5 强刷页面。

### 验证建议

1. 后台打开“邀请码注册”开关。
2. 普通邮箱注册，不填邀请码。
3. 预期：可以继续注册流程，不应出现 `invitation code is required`。
4. 填写有效邀请码注册。
5. 预期：注册成功，并尝试绑定邀请关系。
6. 填写无效邀请码注册。
7. 预期：如果明确填写了无效邀请码，应仍提示邀请码无效。

---

## 5. 线上首个图片生成 Bug 修复：`response_format: url` 挂死

### 来源文档

- `bug记录排查/BUG-REPORT.md`
- 调试记录：`debug-image-url-hang.md`

### 客服反馈现象

外部客户端 WorkBuddy 调用 RelayQ：

```http
POST /v1/images/generations
```

模型：`gpt-image-2`

表现：

- `response_format: "b64_json"`：正常返回，约 40 秒，HTTP 200。
- `response_format: "url"`：挂死，30/60/120 秒均超时。
- 不传 `response_format`：默认走 URL 格式，也挂死。
- RelayQ 后台模型测试正常，因为前端测试强制传了 `response_format: "b64_json"`。

### 根因判断

代码排查确认：

- RelayQ 非流式图片响应路径没有下载或代理图片 URL。
- 非流式处理只是读取上游 JSON，然后 `c.Data` 返回。
- 因此问题不是 RelayQ 下载 URL 卡住。
- 真正的问题方向是：外部客户端走了上游 URL/default 响应路径，而这个路径会挂死；后台测试强制 `b64_json` 所以绕过了问题。

### 修改文件

- `backend/internal/service/openai_images.go`
- `backend/internal/service/openai_images_test.go`
- `debug-image-url-hang.md`

### 修复方案

在网关层做兼容转换：

1. 客户端请求 `response_format: "url"` 时：
   - 转发给上游时强制改成 `response_format: "b64_json"`。
2. 客户端不传 `response_format` 时：
   - 转发给上游时也强制改成 `response_format: "b64_json"`。
3. 客户端明确请求 `response_format: "b64_json"` 时：
   - 保持不变。
4. 如果客户端原本请求的是 URL 或默认格式：
   - RelayQ 收到上游 `b64_json` 后，将 `data[].b64_json` 转换成 `data[].url`。
   - 返回的 `url` 是 `data:image/...;base64,...` data URI。
   - 删除响应项里的 `b64_json` 字段，以保持 URL 响应兼容。

### 关键代码位置

- 请求转发体构建：`backend/internal/service/openai_images.go`
  - `buildOpenAIImagesForwardBody`
- 非流式响应处理：`backend/internal/service/openai_images.go`
  - `handleOpenAIImagesNonStreamingResponse`
- b64 转 data URL：`backend/internal/service/openai_images.go`
  - `convertOpenAIImagesB64JSONToDataURL`
  - `cleanOpenAIImageBase64ForDataURL`
  - `detectOpenAIImageBase64MimeType`

### 新增测试

文件：`backend/internal/service/openai_images_test.go`

新增/覆盖：

- `TestConvertOpenAIImagesB64JSONToDataURL`
- `TestBuildOpenAIImagesForwardBodyNormalizesURLToB64JSON`
- `TestBuildOpenAIImagesForwardBodyDefaultsToB64JSON`

### 已通过测试

在 `backend` 目录运行：

```powershell
go test ./internal/service -run "Test(ConvertOpenAIImagesB64JSONToDataURL|BuildOpenAIImagesForwardBody|OpenAIGatewayServiceParseOpenAIImagesRequest)"
```

结果：

```text
ok github.com/Wei-Shaw/sub2api/internal/service
```

### 已知情况

曾运行：

```powershell
go test ./internal/service ./internal/handler ./internal/server/routes
```

结果：

- `internal/handler` 通过
- `internal/server/routes` 通过
- `internal/service` 有失败，但输出中大量为已有模拟错误日志，未确认与本次图片修复相关
- 当前图片修复相关 targeted tests 已通过

### 验证建议

重开会话后，建议重点验证 3 个请求：

#### 1. 显式 URL 格式

```powershell
curl.exe -X POST "http://localhost:8080/v1/images/generations" `
  -H "Authorization: Bearer sk-你的RelayQ-Key" `
  -H "Content-Type: application/json" `
  -d '{"model":"gpt-image-2","prompt":"draw a cute cat","n":1,"response_format":"url"}'
```

预期：

- 不再挂死。
- 返回 HTTP 200。
- 返回 JSON 中包含 `data[0].url`。
- `data[0].url` 形如：

```text
data:image/png;base64,...
```

#### 2. 省略 `response_format`

```powershell
curl.exe -X POST "http://localhost:8080/v1/images/generations" `
  -H "Authorization: Bearer sk-你的RelayQ-Key" `
  -H "Content-Type: application/json" `
  -d '{"model":"gpt-image-2","prompt":"draw a cute dog","n":1}'
```

预期：

- 不再挂死。
- 返回 `data[0].url`。

#### 3. 明确 `b64_json`

```powershell
curl.exe -X POST "http://localhost:8080/v1/images/generations" `
  -H "Authorization: Bearer sk-你的RelayQ-Key" `
  -H "Content-Type: application/json" `
  -d '{"model":"gpt-image-2","prompt":"draw a cute rabbit","n":1,"response_format":"b64_json"}'
```

预期：

- 保持原行为。
- 返回 `data[0].b64_json`。

---

## 6. 需要重开会话后优先做的验证清单

### 后端重启

进入后端目录：

```powershell
cd c:\work\RelayQ-test\backend
```

启动后端：

```powershell
$env:DATA_DIR="C:\work\RelayQ-test\backend\data"; go run ./cmd/server
```

### 前端重启

进入前端目录：

```powershell
cd c:\work\RelayQ-test\frontend
```

启动前端：

```powershell
pnpm run dev
```

### 重点验证顺序

1. `/v1/images/generations` 的 `response_format: "url"` 不再挂死。
2. `/v1/images/generations` 不传 `response_format` 不再挂死。
3. `/v1/images/generations` 的 `response_format: "b64_json"` 保持正常。
4. 注册页邀请码不填也可注册。
5. 可用渠道页图片价格不再显示 token 单价 `$0.00003 / 次`。
6. `/home` 首页 UI 和联系方式显示正常。

---

## 7. 今天涉及的主要文件汇总

### 后端

- `backend/internal/service/openai_images.go`
- `backend/internal/service/openai_images_test.go`
- `backend/internal/service/auth_service.go`
- `backend/internal/handler/available_channel_handler.go`
- `backend/internal/service/channel_available.go`
- `backend/data/config.yaml`

### 前端

- `frontend/src/views/HomeView.vue`
- `frontend/src/views/auth/RegisterView.vue`
- `frontend/src/views/auth/EmailVerifyView.vue`
- `frontend/src/components/channels/SupportedModelChip.vue`
- `frontend/src/api/channels.ts`

### 调试/记录

- `bug记录排查/BUG-REPORT.md`
- `debug-image-url-hang.md`
- `.trae/documents/invitation-code-optional-register-plan.md`

---

## 8. 给下一轮会话的提示词建议

可以直接对新会话说：

```text
请读取 c:\work\RelayQ-test\2026-06-08-更新进度.md，并按照里面的“需要重开会话后优先做的验证清单”帮我逐项验证。重点先验证 /v1/images/generations 的 response_format:url 和省略 response_format 是否还会挂死。
```
